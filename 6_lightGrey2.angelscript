 // A simple function declaration
 
  int AFunction(int a, int b)
  {
    return a + b;
  }

  auto i = 18;         // integer
  auto f = 18 + 5.f;   // f to float
  auto o = getLongObjectTypeNameById(id);
     // avoids redundancy for long type names
